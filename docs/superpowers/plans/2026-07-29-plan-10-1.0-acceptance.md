# Plan 10: 1.0 Acceptance Matrix — Implementation Plan Outline

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Define and execute the 1.0 release acceptance matrix: capability baseline 100% coverage verification, cross-platform acceptance testing, performance benchmarks, security audit, documentation review, and release readiness sign-off.

**Architecture:** A formal acceptance matrix document (`docs/release/1.0-acceptance-matrix.md`) lists every capability, test, benchmark, and audit item with a sign-off column. The matrix is executed across macOS, Windows, and Linux; each item gets a PASS/FAIL/BLOCKED status with evidence (test output, screenshot, audit report). Release 1.0 ships only when all items are PASS or explicitly waived with documented justification.

**Tech Stack:** Vitest (existing test suite), `cargo test` (Rust), manual testing checklist, `scripts/check-*.mjs` (brand/provenance/i18n/legal gates), performance benchmarking tools (TBD).

---

## ⚠️ External Dependencies (BLOCKING)

This plan **cannot be executed** until the following external resources are available:

1. **All prior plans complete** — Plans 1-9 must be done. Plans 1-6 are ✅ Complete; Plans 7-9 are 🚫 Blocked on external deps.
2. **Testing infrastructure on all platforms** — macOS (Intel + ARM), Windows x64, Linux (at least one distro). Physical or VM access needed.
3. **Performance benchmark baselines** — agreed-upon target metrics (startup time, memory, response latency). Currently undefined.
4. **Security auditor** — external security review (self-audit is insufficient for 1.0).

**Status:** 🚫 Blocked on Plans 7-9 + testing infrastructure. Outline only.

---

## Scope

- Capability baseline 100% coverage verification
- Cross-platform acceptance testing (macOS, Windows, Linux)
- Performance benchmarks (startup time, memory, response latency)
- Security audit (credential storage, permission model, remote access)
- Documentation review (user guide, admin guide, troubleshooting)
- Release readiness sign-off

## High-Level Tasks (detailed TDD steps deferred until deps available)

1. **Create acceptance test matrix** — `docs/release/1.0-acceptance-matrix.md` listing every capability from the master design §5, with test ID, platform, expected result, and sign-off column.
2. **Run acceptance tests on macOS, Windows, Linux** — execute the matrix on each platform; record PASS/FAIL with evidence.
3. **Performance benchmarks** —
   - Startup time: cold start to interactive UI (< 3s target)
   - Memory: idle RSS (< 200MB target)
   - Response latency: time-to-first-token for a simple prompt (< 2s target)
   - Session load: time to load a 100-message session (< 500ms target)
4. **Security audit** —
   - Credential storage: verify `AuthStorage` and `credential-adapter.ts` handle secrets safely.
   - Permission model: verify fail-closed behavior across all v1 methods.
   - Remote access: verify Remote Hub (Plan 7) security model.
5. **Documentation review** — user guide, admin guide, troubleshooting; verify all user-visible strings are localized (en/zh/zh-TW).
6. **Release readiness sign-off** — all items PASS or explicitly waived.

## Preparation Work (can be done NOW without external deps)

- Write the acceptance matrix skeleton (`docs/release/1.0-acceptance-matrix.md`) with all capability items from the master design, even though many are currently BLOCKED.
- Define performance benchmark targets (startup, memory, latency) — these are product decisions, not implementation.
- Write a security audit checklist (`docs/release/security-audit-checklist.md`) covering credential storage, permission model, remote access, brand policy, legal baseline.
- Audit existing test coverage: `pnpm test` (831 tests), `cargo test` (384 tests), `node scripts/check-*.mjs` (brand/provenance/i18n/legal). Document what's covered and what's missing.

## Current Test Coverage (as of Plan 6)

| Suite | Count | Status |
|---|---|---|
| Frontend (vitest) | 831 tests | ✅ All pass |
| Rust (cargo test) | 384 tests | ✅ All pass (1 ignored) |
| Brand policy | 9 tests | ✅ All pass |
| i18n completeness | 5 tests | ✅ All pass |
| Standalone checks | brand/provenance/i18n/legal | ✅ All pass |

## Acceptance Gates (must pass before 1.0)

1. `pnpm test` — 0 failures
2. `cargo test --manifest-path src-tauri/Cargo.toml` — 0 failures
3. `node scripts/check-brand-policy.mjs` — 0 violations
4. `node scripts/check-provenance.mjs` — exit 0
5. `node scripts/check-i18n-completeness.mjs` — exit 0
6. `node scripts/check-legal-baseline.mjs` — exit 0
7. Manual acceptance matrix — all items PASS or waived
