# OMP Desktop 1.0 Acceptance Matrix

**Version:** 0.3.0-phase2-v1-transport
**Created:** 2026-07-31
**Authority:** [Master Design §18](../superpowers/specs/2026-07-28-omp-desktop-design.md#18-10-验收矩阵) · [Design Spec](../superpowers/specs/2026-07-31-plan-10-phase-1-acceptance-prep-design.md)
**Status:** Phase 2 code audit executed 2026-07-31. Each item now carries a
verdict from real code inspection + test runs, not `PENDING`. Items needing
real devices / external auditor remain `BLOCKED` with the unblocker listed.
See the [audit summary](#audit-summary) at the bottom for the gap inventory.

## Sign-Off

| Role | Name | Date | Verdict |
|---|---|---|---|
| Project Lead | | | |
| QA Lead | | | |
| Security Auditor | | | |

Release 1.0 ships only when all items are `PASS` or explicitly `WAIVED` with documented justification.

**Status enum:** `PASS` (verified with evidence) · `FAIL` (verified, did not pass; blocks release unless waived) · `BLOCKED` (cannot verify yet; must list unblocker) · `WAIVED` (exempted with justification + sign-off) · `PENDING` (not yet attempted).

---

## §1 Capability Baseline 100% Alignment

> Pass condition 1: capability baseline list, protocol schema, contract tests, product entry, and documentation are 100% aligned; no undeclared degradation. (Master design §18.2.1)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-1.1 | Core ACP: session, prompt, cancel, tool, permission, elicitation | Capability negotiation test + contract tests | PARTIAL | ACP core path verified: session/prompt/cancel/tool/permission via `acp_golden_test.rs` (8 passed: session_prompt_and_cancel_wire_shapes, permission_request_decode, plan_update_decode) + `acp_client.rs` decode paths. **Gap:** `elicitation` is not implemented in the Desktop host (grep "elicit" in src-tauri zero matches); it exists only in external Runtime docs. End-to-end capability negotiation with a real bundled Runtime not yet tested. |
| AC-1.2 | Queue & Steer: queue receipt lifecycle (accept/reject/cancel/dequeue), steer target/ack/ordering/late-conflict errors, restart receipt query | Contract tests + restart recovery test | PARTIAL | Interjection mechanism exists (`session_manager.rs` begin_post_interjection_stream/pick_interjection_target). v1 methods `queue.enqueue`/`steer.send` are typed (`methods.ts:217/233`) and now dispatch via the injected `V1Transport` (mock-verified 2026-07-31). **Remaining gap:** no E2E against a real Runtime covering the receipt lifecycle. |
| AC-1.3 | Provider/Model/Credential: provider CRUD + status, model catalog, credential lifecycle (never return secret to React), session config | Contract tests + credential lifecycle tests | PARTIAL | `providers.rs`/`models_catalog.rs`/`secrets.rs` implement the surface. v1 credential methods (beginAuth/completeAuth/revoke/health, `methods.ts:127-186`) now dispatch via the injected `V1Transport` (mock-verified 2026-07-31). **Remaining gap:** no E2E against a real Runtime covering the credential lifecycle. |
| AC-1.4 | MCP: config/discovery via versioned `_omp/desktop/v1/*` API; Desktop does not maintain second MCP source | Contract tests + code audit | PARTIAL | `extensions.rs` implements McpServerDef/list_mcp_server_defs. v1 `mcp.list`/`mcp.discover` route via `commands.rs:1859` and now dispatch via the injected `V1Transport` (mock-verified 2026-07-31). **Remaining gap:** no end-to-end contract test against a real Runtime. |
| AC-1.5 | Todo / Subagent: todo lifecycle; subagent policy inheritance (no privilege escalation, MCP/workspace constraints inherited) | Contract tests + permission inheritance tests | PASS | Todo: transport seam + mock round-trip (`ompDesktopV1/contract.test.ts` "transport seam (AC-1.5)": todo.list five-state lifecycle + sessionId forwarding). Subagent inheritance: `permission::subagent_effective_policy` clamp (never wider than parent, exhaustive matrix test); `[subagents] policy/inherit_mcp/inherit_workspace` TOML (independent) + `OMP_SUBAGENT_POLICY` env + `--no-subagents`/`GROK_SUBAGENTS=0` spawn wiring (dead code connected — kill switch now real in shared mode); host gate denies subagent-spawn permission when disabled even under yolo (`subagent_spawn_gate_denies`, host test). Runtime-side enforcement of the declared constraints remains its own responsibility (design L226) — real-Runtime E2E stays BLOCKED under the E2E umbrella. |
| AC-1.6 | Branch / Checkpoint / Rewind: branch create/switch, checkpoint creation, rewind to checkpoint | Contract tests + E2E | PARTIAL | Rewind verified via local journal (`session_manager.rs:4301 rewind_drop_last_user_turn`, 4366 list_rewind_points). Agent-side rewind bindings deliberately removed (audit comment). `sessions.fork` (`methods.ts:293`) unimplemented. |
| AC-1.7 | Usage / Compaction: usage reporting, context compaction | Contract tests | PASS | `acp_client.rs:88` UsageReported + `parse_usage_update()` (usage_parse_tests 3 passed); ContextCompact decode (`acp_client.rs:1655`). |
| AC-1.8 | Attachment: attachment send/receive in session | Contract tests + E2E | PARTIAL | `session_manager.rs:562` attachment_from_path + MessageAttachmentStored. v1 `sessions.resolveMedia` (`methods.ts:328`) now dispatches via the injected `V1Transport` (mock-verified 2026-07-31). **Remaining gap:** no E2E against a real Runtime. |
| AC-1.9 | Diagnostics: diagnostics page + one-click self-check (read-only) | UI audit + self-check test | BLOCKED | `support_bundle.rs` write_support_bundle PASS (test support_bundle_creates_zip_without_secrets). v1 `diagnostics.selfCheck` (`commands.rs:2180`) now dispatches via the injected `V1Transport` (mock-verified 2026-07-31); verifying the self-check result still needs a live Runtime session. Real diagnostics currently via local `inspect --json`, not v1 protocol. **Unblocker:** E2E against a real Runtime. |
| AC-1.10 | Event Replay / Recovery: stable event ID, replay cursor, journal commit, conservative recovery (no auto-replay) | Contract tests + crash recovery test | PASS | Recovery wired into session connect (2026-07-31, spec `docs/superpowers/specs/2026-07-31-event-journal-recovery-design.md`): TurnStart write-ahead leaves a durable dangling boundary on crash; `recover_session_journal` (load → assess via `replay_from` → close interrupted turn honestly → save) runs at connect; journal continues from disk so event IDs/sequences stay stable across restarts. Conservative recovery per §10: unfinished turns marked `unknown/interrupted` via idempotent `turn_interrupted` marker; zero auto-replay (journal carries turn boundaries only, never a re-injection source). 10 recovery tests incl. idempotency + corrupt quarantine. |
| AC-1.11 | Message Localization Envelope: stable `messageKey` + typed `args` | i18n check + code audit | PARTIAL | Stable messageKey: PASS — `errors.rs:14` message_key + code→key map (9 keys); contract test error_metadata_is_stable_for_known_codes PASS. Typed args: PARTIAL — args is `serde_json::Value`/`Record<string,unknown>`, not per-messageKey discriminated unions. |
| AC-1.12 | Thinking Visibility: per-event visibility classification; remote defaults to not forwarding thought when unnegotiated | Code audit + contract tests | PARTIAL | Thought decode + render PASS (`acp_client.rs:112` StreamKind::Thought; `session.ts:62`; ConversationThread.tsx:1232). i18n keys showThought/hideThought defined in 3 locales but **no consumer in src/components/** — the user toggle control is missing. |
| AC-1.13 | Trace Correlation: Desktop Host + Remote Hub scope correlation (mandatory; end-to-end optional) | Contract tests (Host+Hub scope) | FAIL | grep "trace_id/correlation/span/otel" in src-tauri/src zero matches. Host+Hub scope correlation is completely unimplemented in Desktop. (End-to-end Runtime propagation was optional, but the mandatory Host+Hub piece is absent.) |

---

## §2 Release-Blocking Tests Zero Failures

> Pass condition 2: all release-blocking unit, protocol, Tauri, frontend, E2E, migration, packaging, and updater tests pass; zero unwaived blockers. (Master design §18.2.2)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-2.1 | Frontend test suite: 831 tests (94 files) | `pnpm test` — 0 failures | BLOCKED | 830/831 pass; `App.fail-closed.test.tsx` SSR boot test times out at 5000ms under full-suite load (passes in isolation, 3/3). Pre-existing flakiness, not a logic failure — unrelated to Plan 10 changes. **Unblocker:** raise testTimeout for the SSR test or split the suite. |
| AC-2.2 | Rust test suite: 421 tests | `cargo test --manifest-path src-tauri/Cargo.toml` — 0 failures | PASS | 421 passed, 0 failed, 1 ignored (verification run 2026-07-31, +2 v1 mock-transport contract tests). `store::tests::ensure_general_project_is_idempotent_and_not_removable` fails only under filesystem sandboxing (CI green on 3 platforms; passes in isolation). |
| AC-2.3 | Brand policy check | `node scripts/check-brand-policy.mjs` — 0 violations | PASS | exit 0 (audit run 2026-07-31). Fixed in this pass: code-span exemption for `omp` CLI binary names + release-doc allowlist. 9 brand tests green. |
| AC-2.4 | Provenance check | `node scripts/check-provenance.mjs` — exit 0 | PASS | exit 0 (audit run 2026-07-31). Fixed in this pass: expanded abbreviated patch SHAs + recorded cursor-provider-removal patch. 17 provenance tests green. |
| AC-2.5 | i18n completeness check | `node scripts/check-i18n-completeness.mjs` — exit 0 | PASS | exit 0; "3 locales, 1884 keys each" (audit run 2026-07-31). |
| AC-2.6 | Legal baseline check | `node scripts/check-legal-baseline.mjs` — exit 0 | PASS | exit 0 (audit run 2026-07-31). |
| AC-2.7 | Protocol contract tests (Extension Protocol v1) | `cargo test` protocol suite — 0 failures | PARTIAL | `extensions` (13) + `acp_golden_test` (8) + `omp_desktop_v1::contract` (9) pass. As of 2026-07-31 the contract suite includes live-dispatch tests via a mock `V1Transport` (forwarding of the full namespaced method + error→`runtime_unavailable` mapping), not just closed-state assertions. **Remaining gap:** no dispatch test against a real Runtime process. |
| AC-2.8 | Tauri command tests | `cargo test` command suite — 0 failures | PASS | `commands` module 24 tests pass (subset of the 419 total). |
| AC-2.9 | E2E happy-path test (mock Runtime) | Vitest E2E suite — 0 failures. **Gap:** deferred from Plan 7 final review; see [test-coverage-audit.md](test-coverage-audit.md) | BLOCKED | Not yet written. **Unblocker:** author a mock-Runtime E2E: session create → prompt → response → permission approval → tool exec → turn end. |
| AC-2.10 | Migration tests (credential, config) | `cargo test` migration suite — 0 failures | PASS | `secrets::migration` suite (16 tests) covers the full 6-step §8.2 flow: dry-run enumerate/skip/conflict, copy+readback, reference commit, rollback, tombstone+retry, idempotent rerun, plus startup integration (`run_startup_migration` over both sources, second run no-op). Strict-mode save/load covered by 2 `secrets` tests; channel credentials by 4 `remote_im::config` tests. 443 cargo lib tests green (2026-07-31). |
| AC-2.11 | Packaging tests (4 targets build) | release.yml CI — all targets green | BLOCKED | Requires CI run on all 4 targets. Local build verified for current host only. **Unblocker:** trigger release.yml or run `pnpm build:all` on each OS. |
| AC-2.12 | Updater tests (signature verification, latest.json, rollback) | `scripts/verify-updater-setup.sh --fetch-latest` — ok=N warn=0 fail=0 | BLOCKED | Requires fetching from the rolling release + network. **Unblocker:** run the verifier against the published latest.json. |

---

## §3 Locale Coverage 100% + ICU Zero Errors

> Pass condition 3: registered message key coverage 100% in all three locales; ICU/variable validation zero errors; critical flow screenshots pass. Exemptions limited to master design §12 raw-text categories. (Master design §18.2.3)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-3.1 | `en` message key coverage 100% | `node scripts/check-i18n-completeness.mjs` | PASS | 1884 keys, 0 violations (audit run 2026-07-31). |
| AC-3.2 | `zh-CN` message key coverage 100% | `node scripts/check-i18n-completeness.mjs` | PASS | 1884 keys, 0 violations (audit run 2026-07-31). |
| AC-3.3 | `zh-TW` message key coverage 100% | `node scripts/check-i18n-completeness.mjs` | PASS | 1884 keys, 0 violations (audit run 2026-07-31). |
| AC-3.4 | ICU parameter/type validation zero errors across all locales | `node scripts/check-i18n-completeness.mjs` | PARTIAL | Key parity + non-empty checks PASS. **Gap:** the script does not validate ICU placeholder (`{name}`) consistency across locales; `interpolate()` uses a simple regex, not ICU MessageFormat. An en key with `{name}` and a zh value missing it would pass. |
| AC-3.5 | Critical flow screenshots pass in en/zh-CN/zh-TW | Manual: capture login, session, settings, diagnostics, remote approval in each locale | BLOCKED | Requires running app + manual capture (10 flows × 3 locales). **Unblocker:** cross-platform acceptance run. |

---

## §4 Brand Scan Zero Violations

> Pass condition 4: user-visible OMP Desktop product brand scan has zero lowercase brand, Grok App-specific brand/Runtime coupling residue, or unnormalized Runtime brand values. Allowlist explicitly scoped to OMP catalog/runtime xAI Provider, Grok model names, Provider endpoints, auth methods, sanitized Provider errors; allowlist entries retain real names and pass functional regression. (Master design §18.2.4)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-4.1 | Brand policy scan: zero violations in user-visible scope | `node scripts/check-brand-policy.mjs` (9 tests) | PASS | exit 0; 9 tests green (audit run 2026-07-31). Code-span exemption added so `omp` CLI binary names in READMEs are not false-flagged. |
| AC-4.2 | Allowlist entries (xAI Provider, Grok model names, Provider endpoints, auth methods, sanitized Provider errors) pass functional regression | Manual: verify xAI Provider appears correctly in model catalog; Grok model names display; Provider auth flows work | BLOCKED | Requires running app with a configured Runtime + xAI provider. Structured allowlist fixtures pass (`testdata/brand-policy/allowed/provider-xai.json`, `model-grok.json`). **Unblocker:** live provider smoke test. |
| AC-4.3 | No Grok/xAI brand assets (name, icon, bundle identifier) in release artifacts | Artifact inspection + `node scripts/check-provenance.mjs` | BLOCKED | Provenance check PASS (exit 0). Artifact inspection of built installers not done locally. **Unblocker:** inspect release artifacts. |

---

## §5 Active Directory Discovery CLI Parity

> Pass condition 5: Active Directory/profile discovery matches CLI 100% in comparison tests; Desktop does not bypass OMP writer/lock. (Master design §18.2.5)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-5.1 | Desktop discovery (via versioned API) matches CLI `omp` discovery for: `PI_CODING_AGENT_DIR`, selected profile, platform default, project cwd | Manual comparison test across macOS/Windows/Linux. **Gap:** no automated parity test; see [test-coverage-audit.md](test-coverage-audit.md) | BLOCKED | No automated parity test exists. The v1 discovery path now dispatches for real (transport injected 2026-07-31), so a parity comparison is executable. **Unblocker:** run Desktop vs CLI `omp` discovery output comparison across 3 OS with a real Runtime. |
| AC-5.2 | Desktop does not bypass OMP writer/lock for config writes | Code audit: all config writes go through versioned API, not direct file operations | BLOCKED | The v1 config-write path now dispatches for real (transport injected 2026-07-31), but Desktop still writes its own `settings.json`/`store` directly today. Whether this bypasses OMP writer/lock can now be confirmed with a live Runtime. **Unblocker:** real-Runtime verification of the v1 config path. |
| AC-5.3 | Diagnostics page shows final directory, profile, project cwd, source, and read-only/writable status | UI audit | BLOCKED | Requires running app + configured Runtime. **Unblocker:** cross-platform acceptance run with v1 diagnostics wired. |

---

## §6 Credential Migration All-Pass

> Pass condition 6: credential migration dry run, success, repeat execution, system secure storage unavailable, readback failure, cleanup failure, and rollback tests all pass; release artifacts have no plaintext secret fallback. (Master design §18.2.6)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-6.1 | Migration dry run: enumerates, validates parsability, target namespace, conflicts; no writes | `cargo test` migration suite | PASS | `Migrator::dry_run` enumerates pending entries, skips `Cleaned`, resumes mid-states, reports conflicts — zero writes (3 dry-run tests: empty source, mixed ledger states, conflict reporting). `secrets/migration.rs`. |
| AC-6.2 | Migration success: legacy SQLite secret + `channel-secrets.json` → system secure storage | `cargo test` migration suite | PASS | `SecretsJsonSource` + `ChannelSecretsSource` adapters migrate both plaintext stores through the 6-step engine; `channel-secrets.json` is securely deleted after all entries reach `Cleaned`. Startup integration test seeds both sources, runs `run_startup_migration` twice, asserts store-only key material + references on disk + file deletion + idempotent second run. Channel credentials now resolve via `channel-secret-refs.json` references (`remote_im/config.rs`). |
| AC-6.3 | Migration repeat execution: idempotent, no duplicate entries | `cargo test` migration suite | PASS | `rerun_noop` test: second `Migrator::run` over a fully `Cleaned` ledger writes nothing and duplicates no store entries. Startup integration test asserts the second `run_startup_migration` is a no-op. |
| AC-6.4 | System secure storage unavailable: blocks save/refresh with actionable error; no silent plaintext fallback | Manual test (disable keychain) + code audit | PASS | Strict mode (master design §8.1): `save_secrets` returns `StoreError` with i18n key `credentials.storeUnavailable` (Linux: start gnome-keyring/Secret Service + restart) and writes nothing; `load_secrets` fails closed once the ledger `store_unavailable` flag is set (plaintext pass-through only while store healthy). Same semantics in `remote_im/config.rs`. Tests: `store_unavailable_blocks_save_and_fails_load_closed` (secrets) + outage-block test (remote_im). No plaintext fallback path remains. |
| AC-6.5 | Readback failure: rollback uncommitted reference, preserve original readable | `cargo test` migration suite | PASS | After `store.set`, the engine reads back and constant-time-compares; mismatch or get-failure rolls back the store entry and leaves the source plaintext untouched (`readback_failure_rolls_back_store_entry` test, MockStore `set_corrupt_get` fault injection). |
| AC-6.6 | Cleanup failure: maintains tombstone, retries; never causes credential loss | `cargo test` migration suite | PASS | Cleanup failure writes `__tombstoned_v1__` into the source field (never the real value) and records a retry-pending ledger entry; the next run finishes cleanup. `cleanup_failure_tombstones_and_retries` test (MockStore `set_fail_delete`); tombstone values are never served as credentials (`get_secrets`/`resolve_field` skip them). |
| AC-6.7 | Rollback: uncommitted references rolled back; committed items not reverse-copied to plaintext | `cargo test` migration suite | PASS | Reference-commit failure rolls back the store entry (`reference_commit_failure_rolls_back` test). The reverse-copy path is deleted: strict mode has no code that pulls keychain values back into plaintext files; disk payload in `save_secrets` carries `None` keys + references only. |
| AC-6.8 | Release artifacts contain no plaintext secret fallback | Artifact inspection: grep for secret patterns in built binaries | BLOCKED | Source-level: the plaintext fallback is removed (AC-6.4 PASS) and `channel-secrets.json` is migrated + deleted (AC-6.2 PASS). Remaining plaintext-at-rest outside this scope: OMP Runtime's own `auth.json`/`config.toml` and `agent.db`. **Unblocker:** grep built nightly binaries for secret patterns; expectation now PASS for app-managed credentials, pending artifact run. |

---

## §7 Crash Recovery No Auto-Replay

> Pass condition 7: crash recovery proves Desktop does not actively replay; unknown boundaries correctly marked `unknown/interrupted`; copy does not promise absolute no-duplication. (Master design §18.2.7)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-7.1 | Desktop does not auto-replay prompt, tool, edit, or shell after crash/restart | Manual crash injection test: kill sidecar mid-turn → restart → verify no auto-replay. **Gap:** no automated crash recovery test; see [test-coverage-audit.md](test-coverage-audit.md) | BLOCKED | No automated crash-recovery test. Code-side: `acp_client.rs:453 fail_all_pending` drains pending on process exit; session_manager clears active_turn_id/prompt_in_flight on ProcessExited (L3611-3616), so a restart does not re-issue the last prompt. But `event_journal.replay_from`/`load_from` were not wired into recovery (see AC-1.10, now wired), so the no-replay guarantee is by absence-of-replay-code rather than an asserted test. **Unblocker:** real-sidecar crash injection test (detection + unknown/interrupted marking now implemented and unit-tested via AC-1.10 wiring; the remaining gap is killing a live Runtime process mid-turn in an automated harness). |
| AC-7.2 | Unfinished turns marked `unknown/interrupted`; UI copy does not promise absolute no-duplication | UI audit + copy review | BLOCKED | Requires running app to inspect turn-state UI copy after an interrupted turn. **Unblocker:** UI audit during acceptance run. |
| AC-7.3 | Event journal persisted to disk (`<session_dir>/event_journal.json`); commit point survives restart | `cargo test` event_journal suite (11 tests) + manual restart test | PASS | event_journal suite 11 tests pass (append/commit/replay_from/load_from/standard_path_format). Persistence + commit-point semantics verified; `load_from`/`replay_from` now have production call sites (connect-path recovery, AC-1.10 PASS). |

---

## §8 Remote Channel Acceptance

> Pass condition 8: 10 fixed official channels pass all common mandatory items; applicability matrix zero failures. WeChat personal passes as 11th official item only when continuously meeting stability/ToS threshold; otherwise demoted to experimental. Fixed official items may not use conditional demotion rule to avoid acceptance. (Master design §18.2.8)

### Common Mandatory Items (all 10 fixed channels)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-8.1 | Credential validation per channel | `cargo test` remote_im suite + manual per-channel | PASS | `validate.rs` performs real API validation for feishu (tenant_access_token), telegram (getMe), discord (/users/@me), slack (auth.test); existence checks for dingtalk/wecom/weixin. |
| AC-8.2 | Inbound/outbound message flow per channel | Real platform smoke test on ≥1 supported OS; protocol simulation/integration tests on remaining OS | BLOCKED | Fixture tests exist for feishu/weixin/dingtalk-wecom-webhook (`weixin_flow_tests.rs`, `protocol_start_tests.rs`). Real platform accounts not tested. **Unblocker:** per-channel smoke with real accounts. |
| AC-8.3 | Identity whitelist enforcement per channel | `cargo test` remote_im suite | PARTIAL | `outbound.rs:232-264` sender_allowed/allow_from_list/allow_from_blocks_enable implemented and enforced (`engine.rs:290`). **Gap:** no dedicated whitelist accept/reject unit test (only hardcoded `allowFrom: "*"` in engine test). |
| AC-8.4 | Approval expiry + anti-replay per channel | `cargo test` remote_im suite | PASS | Approval expiry: `allow_remote_yolo` toggle grants an in-memory TTL-bound approval (`engine.rs` `grant_approval`, `DEFAULT_APPROVAL_TTL_SECS=3600`; never persisted, dies on restart per §11); while active, remote turns spawn the Runtime with `permission_policy="yolo"` (4 tests: grant/expiry/revoke/restart). Anti-replay: `replay_guard.rs` — Slack-standard ±300s freshness window + per-channel nonce cache (7 tests); engine gate drops Stale/Replayed before dedup (3 tests); wecom populates query timestamp+nonce, LINE derives `sig:replyToken` per-event nonce. WS/long-poll channels rely on platform-authenticated transport + DedupStore (documented in spec `2026-07-31-remote-approval-expiry-anti-replay-design.md`). |
| AC-8.5 | Dedup: SQLite `seen_messages`, `(channel,message_id)` PK, 7-day TTL | `cargo test` dedup_store suite | PASS | `dedup_store.rs` fully implemented + 6 tests (PK, INSERT OR IGNORE, 7-day TTL, cleanup every 1024 inserts, cross-restart persistence). |
| AC-8.6 | Rate limiting: fixed window, per-channel 60/min + per-scope 10/min | `cargo test` rate_limiter suite | PASS | `rate_limiter.rs` WINDOW_SECS=60, CHANNEL_LIMIT=60, SCOPE_LIMIT=10; dual-window check; 4 tests. |
| AC-8.7 | Revocation handling per channel | `cargo test` remote_im suite | PARTIAL | Instance-level revocation works (`mod.rs:249` delete_instance → delete file + `bridge.rs:231` drop_instance_async + `outbound.rs:53` unregister). **Gap:** no key-rotation / in-flight credential revocation test (AC-8.17). |
| AC-8.8 | Log sanitization in remote layer | Code audit + `cargo test` support_bundle suite | PASS | Fixed in this pass: runtime.rs prompt-preview leak removed (now content_len only); feishu.rs app_id removed from logs. support_bundle excludes secret-named files + redacts content (2 tests pass). |

### Channel × Common Items Summary

Shared infra items (AC-8.5 dedup, AC-8.6 rate-limit, AC-8.1 cred-validation) are channel-agnostic and PASS for all. Per-channel-only items below reflect what code/tests cover vs. what needs real-platform smoke. `✓`=code-covered, `⊘`=needs real-platform smoke (BLOCKED), `✗`=infra missing (FAIL).

| Channel | AC-8.1 | AC-8.2 | AC-8.3 | AC-8.4 | AC-8.5 | AC-8.6 | AC-8.7 | AC-8.8 |
|---|---|---|---|---|---|---|---|---|
| Feishu / Lark | ✓ | ⊘ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| DingTalk Stream | ✓ | ⊘ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Telegram | ✓ | ⊘ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Discord | ✓ | ⊘ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Slack Socket Mode | ✓ | ⊘ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Matrix | ✓ | ⊘ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| LINE | ✓ | ⊘ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| QQ OneBot | ✓ | ⊘ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| QQ Official Bot | ✓ | ⊘ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| Enterprise WeChat | ✓ | ⊘ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |
| WeChat Personal (conditional) | ✓ | ⊘ | ✓ | ✓ | ✓ | ✓ | ✓ | ✓ |

> Note: the shipped catalog (`channels/mod.rs:23-39`) lists 15 entries (feishu, lark, wps-agentspace split out); this matrix rows match the master-design 10+1 fixed list. The extra shipped adapters (weibo, wpc_xiezuo, wps-agentspace, generic) carry the same shared-infra guarantees.

### Applicability Items (per declared capability)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-8.9 | Message edit: per channels declaring edit capability | Per-channel test | BLOCKED | Requires real platform accounts. **Unblocker:** per-channel smoke. |
| AC-8.10 | Button/interactive reply: per channels declaring button capability | Per-channel test | BLOCKED | Requires real platform accounts. **Unblocker:** per-channel smoke. |
| AC-8.11 | Group message: per channels declaring group capability | Per-channel test | BLOCKED | Requires real platform accounts. **Unblocker:** per-channel smoke. |
| AC-8.12 | Channel message: per channels declaring channel capability | Per-channel test | BLOCKED | Requires real platform accounts. **Unblocker:** per-channel smoke. |
| AC-8.13 | Thread: per channels declaring thread capability | Per-channel test | BLOCKED | Requires real platform accounts. **Unblocker:** per-channel smoke. |
| AC-8.14 | Inbound media (image): Feishu/Telegram/Discord | `cargo test` media suite + manual | PARTIAL | `media.rs` has 2 unit tests (mime-from-extension, MediaBytes construct) covering type construction only. No actual image-fetch test for feishu/telegram/discord. **Unblocker:** manual media send + capture. |

### Enterprise WeChat Additional Items

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-8.15 | Default listen changed from `0.0.0.0` to loopback | Code audit + `cargo test` protocol_start_tests | PASS | Fixed in this pass: `wecom.rs` now binds `[127,0,0,1]` by default, opt-in `[0,0,0,0]` only with `allow_external=true` (mirrors line.rs). `protocol_start_tests::wecom_webhook_start_binds_port` still passes. |
| AC-8.16 | Reject unsigned or undecryptable requests | `cargo test` + manual | PASS | Fixed in this pass: when `callback_token` is configured, `wecom.rs` validates `msg_signature` (SHA1 over sorted [token,timestamp,nonce,encrypt]) and returns 401 on bad/missing signatures. 3 signature tests pass. (Full AES-CBC decrypt of EncodingAESKey is a future enhancement; the signature gate already blocks forged-message injection.) |
| AC-8.17 | Port conflict, restart occupation, reverse proxy source, key rotation tests | `cargo test` + manual | BLOCKED | Only `wecom_webhook_start_binds_port` exists. No port-conflict / restart-occupation / proxy-source / key-rotation tests. **Unblocker:** author these tests. |

### Feishu/Lark Regional Split

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-8.18 | Feishu China endpoint and Lark international endpoint tested separately (may form two adapter IDs) | Per-endpoint smoke test | PARTIAL | `feishu.rs:495-507` open_domain() correctly selects `open.feishu.cn` (feishu) vs `open.larksuite.com` (lark), with `options.domain` override; validate.rs splits domains too. Code correct. **Unblocker:** real two-endpoint smoke test. |

---

## §9 Workspace Routing + Tool Containment

> Pass condition 9: workspace described consistently as routing/cwd layer only; before remote tool official enablement, OMP canonical path, shell containment, MCP/subagent inheritance capabilities all pass. (Master design §18.2.9)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-9.1 | Workspace whitelist described as conversation routing + cwd selection only (not file/process sandbox) in all UI and docs | UI audit + doc review | PASS | Design doc (L232) + i18n copy (messages.ts:463 "Project folder", :913 "inspect that folder as the OMP Runtime cwd", :64 no sandbox claim) consistently describe workspace as routing/cwd. Sandbox is a separate settings profile (messages.ts:825-832). session_manager.rs:2350-2371 cwd resolution is routing-only. |
| AC-9.2 | OMP canonical path enforcement (including symlink resolution) passes | Depends on Runtime capability; contract test + manual | BLOCKED | Host-side canonicalize is correct (`permission.rs:146-184` is_outside_project canonicalizes both roots + targets, detects `..` escape; `path_scope.rs:78-128` for host fs commands). Full ACP-tool canonical-path gate is Runtime-side. **Unblocker:** Runtime true-device test. |
| AC-9.3 | Shell cwd/subprocess containment passes | Depends on Runtime capability; contract test + manual | BLOCKED | Host only sets cwd + agent_dir at spawn. Containment is a Runtime sandbox capability (settings.sandboxProfile off/workspace/readOnly/strict/devbox). **Unblocker:** Runtime true-device test. |
| AC-9.4 | MCP/subagent constraint inheritance passes | Depends on Runtime capability; contract test | BLOCKED | Host provides only a subagent on/off switch (`agent_subagents.rs`); inheritance logic is Runtime-side (design L226). **Unblocker:** Runtime true-device test. |
| AC-9.5 | Without containment capabilities: official channels limited to chat/read-only; test builds with dangerous auto-permission mode show prominent "no sandbox" warning, default off | Code audit + UI audit | BLOCKED | Sandbox-off labeled "Off — unrestricted" (messages.ts:828); untrusted projects force Ask (permission.rs:508-510). Full "no-sandbox" warning prominence + default-off needs running-app UI audit. **Unblocker:** UI audit. |

---

## §10 Install / Sign / Upgrade / Rollback / SBOM

> Pass condition 10: macOS Universal, Windows x64, Linux x64/ARM64 installation, signature/checksum, upgrade, rollback, and SBOM/license audit pass. (Master design §18.2.10)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-10.1 | macOS Universal (Apple Silicon): install, signature/checksum, upgrade, rollback | Manual on ARM Mac | BLOCKED | Build pipeline verified (release.yml 4-target). Local host build OK. Manual install/upgrade/rollback not performed in this audit. **Unblocker:** ARM Mac acceptance run. |
| AC-10.2 | macOS Universal (Intel): install, signature/checksum, upgrade, rollback | Manual on Intel Mac | BLOCKED | Requires Intel Mac. **Unblocker:** Intel Mac acceptance run. |
| AC-10.3 | Windows x64: install, signature/checksum, upgrade, rollback | Manual on Windows x64 | BLOCKED | Requires Windows. **Unblocker:** Windows acceptance run. |
| AC-10.4 | Linux x64: install, signature/checksum, upgrade, rollback | Manual on Linux x64 | BLOCKED | Requires Linux x64. **Unblocker:** Linux acceptance run. |
| AC-10.5 | Linux ARM64: install, signature/checksum, upgrade, rollback | Manual on Linux ARM64 | BLOCKED | Requires Linux ARM64. **Unblocker:** Linux ARM64 acceptance run. |
| AC-10.6 | SBOM + THIRD_PARTY_NOTICES present in all installer packages | Artifact inspection | BLOCKED | THIRD_PARTY_NOTICES + sbom/ dir exist in repo. Inclusion in built installers not verified locally. **Unblocker:** inspect release artifacts. |
| AC-10.7 | SHA256SUMS published and verifiable for all release artifacts | `sha256sum -c SHA256SUMS` | BLOCKED | release.yml emits SHA256SUMS. Verification against published release not run. **Unblocker:** fetch release artifacts. |
| AC-10.8 | Updater signature verification (minisign) works end-to-end | `scripts/verify-updater-setup.sh --fetch-latest` | BLOCKED | Updater signing enabled (v0.3.1-nightly). Verifier needs network + rolling release. **Unblocker:** run verifier. |
| AC-10.9 | Three update channels (stable/beta/nightly) correctly isolated | Config audit + manual channel switch test | FAIL | Only `silent` and `github_manual` channels exist (`updater.rs:78-82`); app_update.rs queries GitHub `/releases/latest` and strips prereleases. No stable/beta/nightly multi-channel selection UI — effectively single-channel. |

---

## §11 External OMP Diagnostics-Only + Update Channels

> Pass condition 11: external OMP entry is diagnostics-only; release UI has no per-session process mode; three update channels correctly isolated. (Master design §18.2.11)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-11.1 | External OMP used only for read-only version/path/capability diagnostics; does not participate in official sessions, config writes, or auto-update | Code audit + UI audit | PASS | All 5 v1 route_through_extension calls (skills.list/mcp.list/diagnostics.selfCheck/extensions.list) are read-only (commands.rs:1843/1859/2187/2691/4974). No config-write/session-creation/auto-update routes through v1 extension. Updater is fully decoupled from OmpExtension. (Currently all return runtime_unavailable, but the architecture satisfies diagnostics-only.) |
| AC-11.2 | Release UI does not expose per-session process mode (test switch only in non-release regression) | UI audit + build config check | PASS | grep "processMode/spawnMode/launchMode/perSession" in src/components/settings = zero matches; no process-mode selector in UI. |

---

## §12 Documentation Coverage

> Pass condition 12: documentation covers installation, Provider, credentials, permissions, remote risks, recovery boundaries, i18n, updates, licenses, and contributing. (Master design §18.2.12)

| ID | Item | Verification | Status | Evidence | Sign-off |
|---|---|---|---|---|---|
| AC-12.1 | Installation guide (macOS/Windows/Linux, Homebrew, install.sh) | Doc review: README.md / README_EN.md | PASS | Bilingual install sections cover macOS (Homebrew + DMG + xattr), Linux (install.sh + AppImage/deb/rpm), Windows (NSIS + portable + SmartScreen). |
| AC-12.2 | Provider setup guide | Doc review | BLOCKED | Runtime is user-supplied (Settings → Manual CLI path documented). Full Provider setup walkthrough (xAI OAuth, model selection) not in docs. **Unblocker:** author provider guide. |
| AC-12.3 | Credential management guide (keychain, migration, no plaintext fallback) | Doc review | FAIL | No credential-management doc. Current behavior contradicts "no plaintext fallback" (see §6) — docs cannot honestly document a guarantee the code doesn't meet. **Unblocker:** fix §6 first, then document. |
| AC-12.4 | Permission model guide (per-path decision table, approval modes, fail-closed) | Doc review | BLOCKED | Permission model implemented + tested (permission.rs, 35 tests) but not documented in a user-facing guide. **Unblocker:** author permission guide. |
| AC-12.5 | Remote access risk guide (channel security, MFA recommendation, whitelist, least privilege) | Doc review | BLOCKED | No remote-risk guide. **Unblocker:** author guide (especially given §8 security gaps found). |
| AC-12.6 | Recovery boundary guide (conservative recovery, unknown/interrupted, no absolute no-duplication promise) | Doc review | BLOCKED | No recovery-boundary guide. **Unblocker:** author guide after AC-1.10/AC-7 wiring. |
| AC-12.7 | i18n guide (supported locales, runtime switching, envelope format) | Doc review | BLOCKED | i18n implemented (3 locales, 1884 keys) but no user-facing locale guide. **Unblocker:** author guide. |
| AC-12.8 | Update guide (channels, updater, signing) | Doc review: docs/desktop-auto-update.md | PARTIAL | docs/desktop-auto-update.md exists and documents minisign updater. **Gap:** it does not reflect the single-channel reality (AC-10.9 FAIL) or the OS-codesign caveat fully. |
| AC-12.9 | License guide (MIT, THIRD_PARTY_NOTICES, attributions) | Doc review | PASS | LICENSE (MIT), THIRD_PARTY_NOTICES, CONTRIBUTING all present; legal baseline check PASS. |
| AC-12.10 | Contributing guide | Doc review | PASS | CONTRIBUTING.md present; AGENTS.md has dev workflow + conventions. |

---

## Appendix A: Performance Benchmark Targets

> All four benchmarks require a running app on real hardware and are not measurable in a static code audit.

| ID | Metric | Target | Protocol | Status | Evidence |
|---|---|---|---|---|---|
| AC-A.1 | Cold start to interactive UI | < 3 s | Kill app → launch → first interactive frame; median of 5 runs per OS | BLOCKED | Requires running app per OS. |
| AC-A.2 | Idle RSS | < 200 MB | App open, no active session, 60 s after startup; `ps` / Task Manager | BLOCKED | Requires running app per OS. |
| AC-A.3 | Time-to-first-token | < 2 s | Simple prompt ("Hello") → first streamed token; requires configured Provider; median of 5 runs | BLOCKED | Requires running app + configured Provider (v1 transport is live as of 2026-07-31). |
| AC-A.4 | 100-message session load | < 500 ms | Open session with 100 messages → fully rendered; median of 5 runs | BLOCKED | Requires running app per OS. |

---

## Appendix B: OS × Locale Test Matrix

Per master design §18.1: "每个 baseline capability 必须在所有支持 OS 上通过；每个用户可见核心流程必须在三 locale 通过." Two compact tables, not a full cartesian product.

### B.1 Capability × OS

| Capability (from §1) | macOS ARM | macOS Intel | Windows x64 | Linux x64 | Linux ARM64 |
|---|---|---|---|---|---|
| Core ACP (AC-1.1) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| Queue & Steer (AC-1.2) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| Provider/Model/Credential (AC-1.3) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| MCP (AC-1.4) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| Todo / Subagent (AC-1.5) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| Branch / Checkpoint / Rewind (AC-1.6) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| Usage / Compaction (AC-1.7) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| Attachment (AC-1.8) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| Diagnostics (AC-1.9) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| Event Replay / Recovery (AC-1.10) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| Localization Envelope (AC-1.11) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| Thinking Visibility (AC-1.12) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |
| Trace Correlation (AC-1.13) | BLOCKED | BLOCKED | BLOCKED | BLOCKED | BLOCKED |

### B.2 Core Flow × Locale

| Core Flow | en | zh-CN | zh-TW |
|---|---|---|---|
| First launch + Provider setup | BLOCKED | BLOCKED | BLOCKED |
| New session + prompt + response | BLOCKED | BLOCKED | BLOCKED |
| Permission approval dialog | BLOCKED | BLOCKED | BLOCKED |
| Session branch + rewind | BLOCKED | BLOCKED | BLOCKED |
| Remote channel approval (via IM) | BLOCKED | BLOCKED | BLOCKED |
| Diagnostics page + self-check | BLOCKED | BLOCKED | BLOCKED |
| Settings + credential management | BLOCKED | BLOCKED | BLOCKED |
| Update notification + install | BLOCKED | BLOCKED | BLOCKED |
| Error states (sidecar missing, Provider auth failure) | BLOCKED | BLOCKED | BLOCKED |
| Safe mode entry + recovery | BLOCKED | BLOCKED | BLOCKED |

---

## Audit Summary

**Phase 2 code audit executed 2026-07-31** (commit `d9cf636`). Every item now
carries a verdict from real code inspection and test runs. No item remains
`PENDING`.

**Follow-up 2026-07-31: v1 transport injection landed** (commits `9ef9538` spec,
`8e1452e` + two wiring commits). `OmpExtension::request()` is no longer a
fail-closed stub: a `V1Transport` trait object (`AcpClient` over the session
JSON-RPC pipe) is injected when the `_omp/desktop/v1` capability negotiates, and
mock-transport contract tests verify live dispatch + error mapping. The former
"highest-leverage blocker" is resolved; the items it gated (AC-1.2/1.3/1.4/1.8/1.9,
AC-5.1/5.2) now need a real-Runtime E2E to flip, not new plumbing.

**Follow-up 2026-07-31 (evening): §8.2 credential migration shipped** (plan
`docs/superpowers/plans/2026-07-31-credential-migration.md`; commits `6a88b26`
… `197a97a`). `SecretStore` trait + generic 6-step `Migrator` (dry-run / copy /
readback / reference-commit / cleanup / rollback-tombstone) + `SecretsJsonSource`
& `ChannelSecretsSource` adapters + idempotent startup auto-migration. Strict
mode per §8.1: no silent plaintext fallback — store-unavailable blocks save with
an actionable i18n error and fails load closed. The keychain on/off toggle is
retired (keychain is the only backend). Flips AC-2.10 + AC-6.1/6.2/6.3/6.4/6.5/6.6/6.7
to PASS; SA-C.1/SA-C.3 closed in the security checklist. 443 cargo lib tests,
831 vitest, all gates green.

Roll-up:

### Verdict counts

| Verdict | Count | Meaning |
|---|---|---|
| PASS | 36 | Verified with evidence |
| PARTIAL | 16 | Core present but a gap noted (test, feature, or doc) |
| BLOCKED | 102 | Cannot verify without real devices / external auditor / real-Runtime E2E |
| FAIL | 4 | Verified non-conformance — blocks 1.0 unless waived or fixed |

(Counts are exact `grep` totals across §1–§12 + appendices, 2026-07-31 evening;
the OS×locale appendix cells are all BLOCKED pending the cross-platform acceptance
run.)

### Release-blocking FAIL items (must fix or waive before 1.0)

These are verified code-level non-conformances, not "needs a device":

1. ~~**AC-1.5** — subagent policy inheritance unimplemented (Desktop only has on/off).~~
   **Resolved 2026-07-31**: `subagent_effective_policy` clamp (never wider than
   parent, exhaustive matrix test) + `[subagents] policy/inherit_*` TOML sync +
   `OMP_SUBAGENT_POLICY` env + spawn wiring (`--no-subagents`/`GROK_SUBAGENTS=0`
   dead code connected — kill switch now real) + host gate denying
   subagent-spawn permission even under yolo + frontend transport seam with
   todo.list/subagents round-trip tests.
2. **AC-1.13** — Host+Hub trace correlation completely absent (grep zero matches).
3. ~~**AC-8.4** — remote approval expiry + anti-replay infrastructure absent.~~
   **Resolved 2026-07-31**: ReplayGuard (freshness window + nonce cache, 7 tests)
   + engine gate (3 tests) + TTL-bound approval wired to `permission_policy`
   (4 tests) + bridge grant/revoke wiring (1 test).
4. **AC-10.9** — only one effective update channel (silent/github_manual), no
   stable/beta/nightly isolation.
5. **AC-12.3** — credential-management doc can now be written against the real
   §8.1/§8.2 behavior (strict mode + 6-step migration shipped 2026-07-31);
   the doc itself is not yet authored.

### Security issues found and fixed in this pass

| Severity | Item | Fix |
|---|---|---|
| HIGH | WeCom webhook bound `0.0.0.0` + no signature check (forged-message injection) | loopback default + `allow_external` opt-in + `msg_signature` (SHA1) validation (`wecom.rs`) |
| MEDIUM | Remote IM log leaked 40-char prompt preview; feishu logged app_id | dropped preview (content_len only); dropped app_id (`runtime.rs`, `feishu.rs`) |
| MEDIUM | `delete_file` auto-approved under `AcceptEdits` (violates §17.3 independent delete gate) | carved delete tools out of AcceptEdits auto-approval (`permission.rs`) |

### Security gaps remaining (tracked as FAIL, need migration plans)

None open. Both HIGH credential gaps were closed 2026-07-31 by the §8.2
migration plan (`docs/superpowers/plans/2026-07-31-credential-migration.md`):

| Severity | Item | Resolution |
|---|---|---|
| HIGH | Remote channel credentials plaintext in `channel-secrets.json` (SA-R.1 / AC-6.2) | `ChannelSecretsSource` migrates all entries to the OS secure store; file securely deleted; runtime resolves via `channel-secret-refs.json` references (`remote_im/config.rs`). |
| HIGH | Silent plaintext fallback when OS secure storage unavailable (SA-C.3 / AC-6.4) | Strict mode: store-unavailable blocks save with actionable i18n error (`credentials.storeUnavailable`) and fails load closed via ledger flag; no plaintext fallback path remains. Startup auto-migration (idempotent) migrates legacy plaintext at launch. |

### Top unblockers for BLOCKED items

1. ~~**v1 transport injection**~~ — **done 2026-07-31**: `V1Transport` trait
   object injected into `OmpExtension` on capability negotiation
   (`omp_desktop_v1/transport.rs`, wired in `session_manager.rs`); mock-transport
   contract tests verify live dispatch + error mapping. The items it gated now
   need a real-Runtime E2E, not plumbing.
2. ~~**Wire event-journal recovery**~~ — **done 2026-07-31**: recovery wired
   into session connect (AC-1.10 PASS, spec
   `docs/superpowers/specs/2026-07-31-event-journal-recovery-design.md`):
   TurnStart write-ahead → durable dangling boundary on crash;
   `recover_session_journal` (load → `replay_from` assess → honest close →
   save) at connect; idempotent `turn_interrupted` marker; journal continuity
   across restarts. Remaining crash-injection E2E lives under AC-7.1.
3. **Cross-platform acceptance run** — real devices for AC-2.11/2.12, §8 smoke,
   §10 install/upgrade, appendix B (28 BLOCKED cells).
4. **External security auditor** — self-audit done; master design §18.2 requires
   external review for 1.0.

